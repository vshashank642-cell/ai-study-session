# Server-side only. Never commit your real key.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash

# Supabase public browser configuration. These are safe to expose client-side,
# but your Supabase RLS policies must be enabled as provided in supabase/schema.sql.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
